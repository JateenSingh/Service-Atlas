{
  "name": "service-atlas-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end tests for Service Atlas (Q-2)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
