services {
  pricing {
    url = "http://log-pricing-svc:8080"
  }
}


db.default {
  url = "jdbc:postgresql://quotes-db.logistics:5432/quotes"
}

# The same cache log-order-svc uses — shared infrastructure the diagram should show.
redis {
  url = "redis://pricing-cache.logistics:6379/0"
}
