# Service-Atlas
ervice Atlas is a local-first desktop-style web application that scans a user-selected folder containing multiple microservice repositories (initially Scala/SBT services), parses their build definitions and source code to extract inter-service dependencies, and renders an interactive architecture diagram.
